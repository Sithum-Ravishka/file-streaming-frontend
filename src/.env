# ================= FRONTEND ENV =================

# API base URL for the upload server
VITE_SERVER_URL=https://listening.bethel.network

# Default Bearer token for testing (UI lets user override)
VITE_BEARER_TOKEN=demo

# Default chunk/part size (bytes) — clamped by server’s /limits
# e.g. 5 MiB = 5242880
VITE_PART_SIZE=5242880

# Default concurrency values (UI can adjust, but don’t exceed /limits)
VITE_PARALLEL_FILES=3
VITE_CHUNK_WORKERS=4

# Whether to request server-side checksum verification on complete
VITE_FINALIZE_WITH_SHA256=true

# ================= UI BEHAVIOR =================

# Heartbeat interval in ms (matches server’s HB 30s, but keep shorter)
VITE_HEARTBEAT_INTERVAL=25000

# Polling interval for refreshing server-side file status
VITE_STATUS_REFRESH_MS=10000

# Whether to auto-start upload on file drop
VITE_AUTO_START=false

# Enable WebSocket live progress
VITE_USE_WEBSOCKET=true
